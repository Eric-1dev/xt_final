@using Album.PL.Web.Models
@using Album.Entities
@{
    var WebPL = new WebAlbumPL();

    var users = WebPL.GetAllUsers().Where(user => user.Login != User.Identity.Name);
}

<div id="users_list_wrapper" class="m-2 border">
    @foreach (var user in users)
    {
        var avatar = user.Avatar != null ? user.Avatar : "/Images/no_avatar.png";
        <text>
            <div class="m-0 d-flex align-items-center main_users_row" id="@user.Id">
                <div class="right_avatar_wrapper"><img src="@avatar" /></div>
                <div class="text-dark users_list_name">@user.Name</div>
                @if (User.IsInRole("user"))
                {
                    <text>
                        <div class="btn btn-sm btn-link btn_user_edit right_users_edit_but">(?)</div>
                    </text>
                }
            </div>
        </text>
    }
</div>